name: RCE P0-014 Continuity Checkpoint Candidate (Superseded)

# Execution ownership moved to:
#   core_lite/reference_loop.json#REF-LOOP-007
#   tools/reconcile_rce_canonical_workstream.py
# This workflow is retained as historical implementation evidence only. It has
# no trigger and cannot race the canonical reference-loop lease.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by REF-LOOP-007; no execution performed."
