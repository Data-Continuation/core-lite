{
  "schema": "stegverse_validation_rules.v1",
  "version": "0.9.0",
  "validation_direction": "bottom_to_top_and_top_to_bottom",
  "canonical_rule": "Only the most recent validated source may produce ALLOW.",
  "rules": []
}
