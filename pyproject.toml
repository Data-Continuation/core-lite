[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stegverse-core-lite"
version = "0.9.0"
description = "StegVerse Core-Lite ingestion, CGE, continuity, and manifested route carrier"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["core_lite*"]
